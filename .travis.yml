language: python
python:
    - "2.7"
env:
    - SUPERVISOR_PYTHON_VERSION=2.7
install:
    - pip install -e .
    - pip install -e .[tests]
    - ./install_supervisor.sh
script:
    - pre-commit run --all
    - python setup.py test
sudo: false
